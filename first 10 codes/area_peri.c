#include<stdio.h>
int main(){
    int length=5;
    int breadth=10;
    int area , perimeter;
    printf("Area is : %d \n", area=length*breadth );
    printf("Perimeter is :%d", perimeter=2*(length+breadth));
    return 0;
}